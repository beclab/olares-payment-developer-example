/**
 * Platform-specific types — used only by PlatformClient.
 *
 * Currently empty: PlatformClient's two methods (createPayment / getPayment)
 * all use shared types from ./base. Future platform-only types land here.
 */
export {};
